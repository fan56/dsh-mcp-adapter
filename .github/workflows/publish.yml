name: Publish

on:
  push:
    tags: ['v*']

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # npm provenance
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org/

      - name: Install dsh CLI (rolling rc/stable line — newest of latest/next, never alpha)
        run: |
          # Policy (2026-09-03): the alpha line is retired for CI. Resolve the
          # newest of the `latest` (stable) and `next` (rc) dist-tags at
          # runtime, never hand-pinned — when a stable 0.1.2+ lands on
          # `latest` it wins over the rc by plain semver compare. Mirrors
          # ci.yml so the release gates against the same host closure.
          STABLE="$(npm view @deepseek-ai/dsh@latest version)"
          RC="$(npm view @deepseek-ai/dsh@next version 2>/dev/null || true)"
          DSH_VERSION="$STABLE"
          if [ -n "$RC" ] && [ "$(printf '%s\n' "$STABLE" "$RC" | sort -V | tail -1)" = "$RC" ]; then
            DSH_VERSION="$RC"
          fi
          echo "dsh closure: ${DSH_VERSION}"
          npm install -g "@deepseek-ai/dsh@${DSH_VERSION}" --no-audit --no-fund

      - name: Link dsh closure types
        run: node scripts/link-dsh-closure.mjs

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      # Gate the release on the same checks CI runs per push.
      - name: Typecheck
        run: npm run check

      - name: Test
        run: npm test

      - name: Stamp package.json to the tag version
        # The tag is the single source of truth for what gets published (the
        # dsh-tui-pi@1.1.0 flow). Small patches skip the version-bump commit —
        # push the fix, tag it, and the workflow rewrites package.json here
        # (--allow-same-version makes a matching version a no-op). main's
        # package.json may lag behind npm; the next deliberate bump
        # (`npm version minor|major`) starts from whatever main carries, and
        # the stamp keeps publishes tag-accurate.
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          npm version "${TAG_VERSION}" --no-git-tag-version --allow-same-version
          echo "publishing @aiwayds/dsh-mcp-adapter@${TAG_VERSION} (tag ${GITHUB_REF_NAME})"
      - name: Publish
        # A re-pushed tag (or a publish landing mid-run) can race an
        # already-published version. npm versions are immutable, so if the
        # version is already on npm — visible up front via `npm view`, or
        # matched by the registry's "cannot publish over" 403 after the
        # first PUT succeeded — degrade to a warning instead of failing.
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if npm view "@aiwayds/dsh-mcp-adapter@${TAG_VERSION}" version >/dev/null 2>&1; then
            echo "::warning::@aiwayds/dsh-mcp-adapter@${TAG_VERSION} is already on npm — skipping publish"
            exit 0
          fi
          set +e
          PUB_OUT="$(npm publish --access public --provenance 2>&1)"
          PUB_RC=$?
          set -e
          printf '%s\n' "$PUB_OUT"
          if [ "${PUB_RC}" -ne 0 ] && printf '%s' "$PUB_OUT" | grep -q "cannot publish over the previously published versions"; then
            echo "::warning::@aiwayds/dsh-mcp-adapter@${TAG_VERSION} was published concurrently (or landed mid-publish) — treating as success"
            exit 0
          fi
          exit "${PUB_RC}"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
